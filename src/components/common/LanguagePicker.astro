---
import { getLocale } from 'astro-i18n-aut';
import { I18N } from '~/utils/config';
const currentLocale = getLocale(Astro.url);
---

<select
  id="languagePicker"
  class="px-4 py-2 rounded-md text-sm font-medium bg-transparent dark:bg-dark text-gray-700 dark:text-gray-300 focus:ring-1 focus:ring-gray-200 focus:border-gray-200 hover:bg-gray-100 dark:hover:bg-gray-700 transition duration-150 ease-in-out uppercase"
  data-aw-select-language
>
  {
    Object.keys(I18N.locales).map((locale) => (
      <option value={locale} selected={locale === currentLocale} class="py-1">
        {locale}
      </option>
    ))
  }
</select>

